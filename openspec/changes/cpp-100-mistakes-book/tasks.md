## 1. 项目脚手架与规范

- [x] 1.1 创建 `books/cpp-100-mistakes/` 目录
- [x] 1.2 编写 `README.md`：书名《100 个 C++ 语言典型错误》、前言(说明实际收录 100+ 条,约 105 条)、格式说明、完整目录占位（#1–#105 锚点）
- [x] 1.3 确定每条错误的固定条目模板（标题/错误示例/为什么错/正确做法/适用标准/相关准则），写入 README 的"如何阅读"小节
- [x] 1.4 冻结章节编号区间分配（13 章，合计约 105 条），在 README 中记录每章对应的编号范围

## 2. 第 1 章 代码与项目组织（#1–#6）

- [x] 2.1 创建 `ch01-organization.md`
- [x] 2.2 撰写 #1–#6：头文件缺少 include guard/`#pragma once`、在头文件中定义非 inline 函数导致重复定义、`using namespace std;` 置于头文件、模板定义放在 .cpp 中导致链接错误、跨翻译单元的全局对象初始化顺序问题、声明变量却被解析为函数（most vexing parse）
- [x] 2.3 校对本章所有代码示例可编译并标注适用标准

## 3. 第 2 章 基本类型与转换（#7–#15）

- [x] 3.1 创建 `ch02-fundamental-types.md`
- [x] 3.2 撰写 #7–#15：有符号/无符号混用比较、整型溢出为 UB/回绕、隐式窄化转换、浮点数做等值比较、用浮点表示货币、`char` 有无符号不确定、位移超过位宽为 UB、`int` 与 `size_t` 混用、误用 `auto` 丢失引用/const
- [x] 3.3 校对本章所有代码示例可编译并标注适用标准

## 4. 第 3 章 内存管理与 RAII（#16–#25）

- [x] 4.1 创建 `ch03-memory-raii.md`
- [x] 4.2 撰写 #16–#25：`new` 后忘记 `delete` 泄漏、`new[]` 用 `delete` 释放为 UB、重复 `delete` 双重释放、悬垂指针 use-after-free、未初始化指针、栈对象返回其地址/引用、异常路径下手动 delete 泄漏、用裸 `new/delete` 而非 RAII、内存碎片与过度堆分配、`delete` 后未置空
- [x] 4.3 校对本章所有代码示例可编译并标注适用标准

## 5. 第 4 章 资源与所有权（#26–#36）

- [x] 5.1 创建 `ch04-ownership.md`
- [x] 5.2 撰写 #26–#36：违反 Rule of 0/3/5、拷贝 `unique_ptr`、把裸指针与智能指针混用、`shared_ptr` 作为默认指针类型、`shared_ptr` 循环引用未用 `weak_ptr`、工厂函数返回裸拥有指针、对 `unique_ptr` 管理的内存再 `delete`、用 `new` 构造 STL 容器、所有权不清晰(谁负责释放)、`make_shared` 与 `make_unique` 使用不当、在成员函数里用 `this` 构造 `shared_ptr<T>(this)` 产生第二个控制块导致双重释放(正确做法:`enable_shared_from_this` + `shared_from_this()`,并注意不能在构造函数中调用)
- [x] 5.3 校对本章所有代码示例可编译并标注适用标准

## 6. 第 5 章 移动语义与值类别（#37–#45）

- [x] 6.1 创建 `ch05-move-semantics.md`
- [x] 6.2 撰写 #37–#45：使用已 move 的对象、move 构造/赋值未标 `noexcept` 导致容器退化为拷贝、对返回值滥用 `std::move` 抑制 NRVO、对 `const` 对象 `std::move` 静默退化为拷贝、实现了移动构造却漏了移动赋值、移动后未重置源对象状态、按值传参 vs 完美转发混淆、`std::move` 与 `std::forward` 误用、moved-from 状态假设过强
- [x] 6.3 校对本章所有代码示例可编译并标注适用标准

## 7. 第 6 章 类、对象与继承（#46–#55）

- [x] 7.1 创建 `ch06-classes-inheritance.md`
- [x] 7.2 撰写 #46–#55：基类缺少虚析构函数、构造/析构中调用虚函数、对象切片(object slicing)、纯虚函数在构造/析构中调用为 UB、隐藏而非重写(缺 `override`)、拷贝构造中的浅拷贝、成员初始化顺序与声明顺序不一致、自赋值未处理、隐式转换构造未加 `explicit`、以值方式按基类传递派生对象
- [x] 7.3 校对本章所有代码示例可编译并标注适用标准

## 8. 第 7 章 模板与泛型（#56–#64）

- [x] 8.1 创建 `ch07-templates.md`
- [x] 8.2 撰写 #56–#64：忘记 `typename`/`template` 消歧、ADL 引入意外重载、误用 `std::enable_if`/应改用 concepts、转发引用与 `const&` 重载歧义、模板错误信息难读未加约束、显式实例化缺失导致链接错误、对依赖名的查找规则误解、可变参数模板递归展开写错、模板代码膨胀
- [x] 8.3 校对本章所有代码示例可编译并标注适用标准

## 9. 第 8 章 STL 容器与算法（#65–#74）

- [x] 9.1 创建 `ch08-stl.md`
- [x] 9.2 撰写 #65–#74：迭代器失效(插入/删除后继续用)、遍历 `vector` 时改变其大小为 UB、`operator[]` 越界不做边界检查、`map::operator[]` 意外插入默认值、`erase-remove` 惯用法误用、`std::vector<bool>` 特化陷阱、传递失效的 `end()`、算法谓词有副作用/非纯、`reserve` 缺失导致多次重分配、`emplace_back` 与 `push_back` 误用
- [x] 9.3 校对本章所有代码示例可编译并标注适用标准

## 10. 第 9 章 并发（#75–#85）

- [x] 10.1 创建 `ch09-concurrency.md`
- [x] 10.2 撰写 #75–#85：共享数据无同步的数据竞争、只保护写不保护读、原子变量与非原子访问混用、误解 memory order、锁顺序不一致导致死锁、忘记 `join`/`detach` 导致 `std::terminate`、`std::thread`/`std::async` 按引用捕获局部变量导致线程读取悬垂引用、`lambda` 按引用(`[&]`)或按 `this` 捕获后被存入回调/容器/定时器并逃逸出作用域导致悬垂引用(正确做法:按值捕获、捕获 `shared_ptr`/`weak_ptr`,或用 `enable_shared_from_this` 延长生命周期)、条件变量虚假唤醒未循环判断、在析构中做阻塞 I/O、`mutex`/`atomic` 被拷贝或移动
- [x] 10.3 校对本章所有代码示例可编译并标注适用标准

## 11. 第 10 章 错误处理（#86–#93）

- [x] 11.1 创建 `ch10-error-handling.md`
- [x] 11.2 撰写 #86–#93：析构函数抛异常、从 move 操作抛异常、忽略返回的错误码、`catch(...)` 吞掉异常、非异常安全的资源获取顺序、用异常处理正常控制流、`noexcept` 标注不当、跨 DLL/模块边界抛异常
- [x] 11.3 校对本章所有代码示例可编译并标注适用标准

## 12. 第 11 章 标准库与 API 使用（#94–#98）

- [x] 12.1 创建 `ch11-standard-library.md`
- [x] 12.2 撰写 #94–#98：`string_view`/`span` 悬垂(引用临时/已销毁存储)、`c_str()` 生命周期问题、`std::chrono` 单位混用、`rand()`/未播种或误用随机数引擎、`std::endl` 频繁刷新的性能问题
- [x] 12.3 校对本章所有代码示例可编译并标注适用标准

## 13. 第 12 章 性能与优化（#99–#102）

- [x] 13.1 创建 `ch12-performance.md`
- [x] 13.2 撰写 #99–#102：不必要的拷贝(该用 `const&`/move)、循环内 `string +=`/重复分配、忽视缓存局部性与数据布局、未 profiling 的过早优化
- [x] 13.3 校对本章所有代码示例可编译并标注适用标准

## 14. 第 13 章 系统编程与 Linux I/O（#103–#105）

- [x] 14.1 创建 `ch13-systems-linux-io.md`,并在章首说明本章为 Linux 平台相关内容(非可移植标准 C++)
- [x] 14.2 撰写 #103–#105：epoll 的 ET(边缘触发)模式下未循环读到 `EAGAIN`/`EWOULDBLOCK` 导致事件丢失或饥饿、`epoll_ctl` 增删 fd 与 `close(fd)` 之间的竞态(fd 复用导致监听到错误对象;正确做法:先 `EPOLL_CTL_DEL` 再 `close`)、`epoll_event.data.ptr` 指向的对象已析构/已释放导致回调时悬垂指针
- [x] 14.3 校对本章所有代码示例可编译并标注适用标准(注明依赖 `<sys/epoll.h>` 与 Linux 内核版本)

## 15. 整合与终审

- [x] 15.1 核对全书恰好 105 条、编号 #1–#105 连续且无重复、各章区间不重叠
- [x] 15.2 在 README 目录中为全部 105 条补齐可跳转的锚点链接并验证链接可达
- [x] 15.3 统一术语（首次出现给出英文原词）、统一条目模板、消除跨章重复（改为交叉引用）
- [x] 15.4 逐条核对"适用标准/平台"标注准确，确保未推荐目标标准尚未提供的特性,并对 Linux 平台相关条目明确标注
- [x] 15.5 原创性复核：确认无逐字抄袭参考资料，仅借用结构与选题
