#include <chrono>
#include "test_case.h"

class TinyTimer {
public:
    void schedule(const std::chrono::duration& duration,
        const std::chrono::duration& delay);
private:
    +

    -
};

class TinyTimerTest : public BaseTestCase
{
public:

};